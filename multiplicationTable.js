


// Generate Multiplication Table for: 5 start here
function multiplyTable(multiplyBy) {
  for (var i = 1; i <= 10; i++) {
    let k = i * multiplyBy;
    console.log(`${i} x ${multiplyBy} = ${k}`);
  }
}
multiplyTable(5);

// Generate Multiplication Table for: 5 end here
